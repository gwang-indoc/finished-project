# Dev Log — 2026-04-29

### 1. Move Help link to bottom-right of welcome page
**Commit:** `f9ffdaa`

**Feature:**
- Moved the Help link wrapper in `app/page.tsx` from `absolute top-4 right-6` to `absolute right-6 bottom-4`. Inner `<Link>` markup (icon badge + "Help" label + classes + hover state) is unchanged.
- Updated the `welcome-page` capability spec via a `RENAMED` + `MODIFIED` delta (`openspec/changes/move-help-link-to-bottom-right/specs/welcome-page/spec.md`) so the requirement and "User sees Help link" scenario reference the bottom-right corner. The "Help link navigates to `/help`" scenario is unchanged. The delta is merged into the canonical spec at archive time.
- Brainstorming spec captured at `docs/superpowers/specs/2026-04-29-help-link-position-design.md`. Position decision validated via the visual companion (side-by-side before/after mockup).

**Code Review Findings, if any:**
| Severity | Issue | Fix |
|---|---|---|
| _none_ | Group 1 review (`superpowers:code-reviewer`) approved the diff with no CRITICAL/HIGH/LOW findings. Diff is one line and matches `design.md` Decision 1 exactly. | — |

**Pre-existing issue (not introduced by this change):**
- `bun run lint` reports `react-hooks/set-state-in-effect` at `components/share-toggle.tsx:23`. Verified by stashing the diff and re-running lint on a clean tree. To be addressed in a separate openspec change; this batch confirmed it introduces no new lint violations.

**Tests:** 42 tests all passed (4 files), no newly added tests — per `design.md` Decision 3, the change is a positional Tailwind class swap with no DOM-testable behavior change beyond what the existing `welcome-page` scenarios already cover.

## To Do

- [x] Commit this change (`app/page.tsx`, openspec change folder, brainstorming spec) — done in `f9ffdaa`. Dev log committed in a follow-up.
- [x] Run `/opsx:archive` to merge the welcome-page delta into `openspec/specs/welcome-page/spec.md` — done; archived to `openspec/changes/archive/2026-04-29-move-help-link-to-bottom-right/` and main spec synced.
- [ ] Open a separate openspec change to fix the pre-existing `share-toggle.tsx` lint error.
